version: '1.0'
steps:
    unit_test:
      type: composition
      composition:
        version: '2'
        services:
          db:
            image: ubuntu
            container_name: db
          test:
            image: ubuntu
            container_name: 'test.api'

      composition_candidates:
        test:
          image: ubuntu
